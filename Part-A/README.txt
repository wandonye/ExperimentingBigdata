TO RUN APPLICATION:
spark-submit --master spark://10.254.0.142:7077 Part-A-Question-<Question_Number>.py HDFS_PATH_TO_WEB_GRAPH 10

NOTES:
For the statistics reported in the write-up (e.g., completion time), we ran the applications redirecting the final results (i.e., ranks) to a text file instead of printing on screen. Thus you may find it takes longer running the applications in the above way (i.e., printing to screen).